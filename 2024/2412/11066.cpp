#include <iostream>
#include <vector>

using namespace std;

int main()
{
    cin.tie(0)->sync_with_stdio(0);
    int t;
    cin>>t;
    while (t--)
    {
        int n;
        vector<int> v(n+1);
        cin>>n;
        for (int i=1; i<=n; i++)
        {
            cin>>v[n];
        }
        vector<vector<int>> dp(n+1, vector<int> (n+1));
    }
}