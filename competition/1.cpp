#include <iostream>

using namespace std;

int main()
{
    // int n, m;
    // cin >> n >> m;
    // int time = m - (n - 1);
    // cout << time / n;
    // return 0;
    string s="대상혁";
    cout<<s;
    return 0;
}